<%= form_for(@partido) do |f| %>
  <% if @partido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@partido.errors.count, "error") %> prohibited this partido from being saved:</h2>

      <ul>
      <% @partido.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cancha_id %><br />
    <%= f.number_field :cancha_id %>
  </div>
  <div>
    <%= f.collection_select(:cancha_id, @canchas, :id, :nombre) %>
  </div>
  <div class="field">
    <%= f.label :fecha_encuentro %><br />
    <%= f.datetime_select :fecha_encuentro %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
